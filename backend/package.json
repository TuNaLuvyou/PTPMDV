{
  "name": "hrm-backend",
  "version": "1.0.0",
  "private": true,
  "description": "HRM Enterprise Core Server — REST + SOAP payroll gateway (On-Premises SOA)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=18"
  }
}
