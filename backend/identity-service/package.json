{
  "name": "identity-service",
  "version": "1.0.0",
  "private": true,
  "description": "HRM Enterprise identity-service — xác thực JWT + cookie hrm-session (port 4001)",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "node --test tests/unit/*.test.js tests/integration/*.test.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "prisma": "^5.18.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
